use bevy::prelude::*;

#[derive(Debug, Copy, Clone)]
pub struct ColourPalette {
    pub primary: Color,
    pub secondary: Color,
    pub tertiary: Color,
    pub quaternary: Color,
    pub quinary: Color,
}
